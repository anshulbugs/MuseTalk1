# Core streaming dependencies
websockets>=11.0.3
flask>=2.3.3
flask-cors>=4.0.0

# Audio processing
pyaudio>=0.2.11
soundfile>=0.12.1

# Data handling
pyyaml>=6.0.1
numpy>=1.23.5
opencv-python>=4.9.0.80

# Utilities
requests>=2.31.0

# Optional: For enhanced audio processing
librosa>=0.11.0

# Optional: For WebRTC support (advanced)
aiortc>=1.6.0
aiohttp>=3.8.5
aiohttp-cors>=0.7.0

# Note: The following are built-in Python modules and don't need to be installed:
# wave, pathlib, tempfile, base64, logging, threading, queue, time, json, os, shutil, zipfile

# Note: The main MuseTalk dependencies should already be installed
# in the MuseTalk project directory. This file only includes
# additional dependencies for the streaming system.